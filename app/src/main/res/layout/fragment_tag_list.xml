<!-- res/layout/tag_selection_dialog.xml -->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <ListView
        android:id="@+id/listViewTags"
        android:layout_width="375dp"
        android:layout_height="221dp"
        android:layout_gravity="center"
        android:background="@drawable/rounded_background_little"
        android:choiceMode="multipleChoice"
        android:listSelector="@android:color/transparent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:id="@+id/addNewTag"
        android:layout_width="375dp"
        android:layout_height="wrap_content"
        android:hint="태그 입력"
        android:padding="10dp"
        app:layout_constraintStart_toStartOf="@id/listViewTags"
        app:layout_constraintTop_toBottomOf="@id/listViewTags" />

    <Button
        android:id="@+id/buttonAddTag"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end"
        android:text="추가"
        app:layout_constraintTop_toBottomOf="@id/addNewTag" />
</androidx.constraintlayout.widget.ConstraintLayout>
